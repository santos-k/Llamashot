<Window x:Class="Llamashot.Views.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="False"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen"
        MinWidth="320" MinHeight="240"
        KeyDown="Window_KeyDown">

    <Grid>
    <!-- Invisible resize grips on all edges -->
    <Border Background="Transparent" Width="6" HorizontalAlignment="Left" Cursor="SizeWE"
            MouseLeftButtonDown="ResizeEdge_MouseDown" Tag="Left" />
    <Border Background="Transparent" Width="6" HorizontalAlignment="Right" Cursor="SizeWE"
            MouseLeftButtonDown="ResizeEdge_MouseDown" Tag="Right" />
    <Border Background="Transparent" Height="6" VerticalAlignment="Top" Cursor="SizeNS"
            MouseLeftButtonDown="ResizeEdge_MouseDown" Tag="Top" />
    <Border Background="Transparent" Height="6" VerticalAlignment="Bottom" Cursor="SizeNS"
            MouseLeftButtonDown="ResizeEdge_MouseDown" Tag="Bottom" />
    <Border Background="Transparent" Width="10" Height="10" HorizontalAlignment="Right"
            VerticalAlignment="Bottom" Cursor="SizeNWSE"
            MouseLeftButtonDown="ResizeEdge_MouseDown" Tag="BottomRight" />
    <Border Background="Transparent" Width="10" Height="10" HorizontalAlignment="Left"
            VerticalAlignment="Bottom" Cursor="SizeNESW"
            MouseLeftButtonDown="ResizeEdge_MouseDown" Tag="BottomLeft" />
    <Border Background="Transparent" Width="10" Height="10" HorizontalAlignment="Right"
            VerticalAlignment="Top" Cursor="SizeNESW"
            MouseLeftButtonDown="ResizeEdge_MouseDown" Tag="TopRight" />
    <Border Background="Transparent" Width="10" Height="10" HorizontalAlignment="Left"
            VerticalAlignment="Top" Cursor="SizeNWSE"
            MouseLeftButtonDown="ResizeEdge_MouseDown" Tag="TopLeft" />

    <Border x:Name="RootBorder" CornerRadius="10" Margin="3"
            BorderBrush="#33FFFFFF" BorderThickness="1">
        <Border.Background>
            <SolidColorBrush Color="#1A1A2E" Opacity="0.95" />
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.6" Color="Black" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Content Area -->
            <Grid x:Name="ContentArea" Margin="2" ClipToBounds="True">
                <Border x:Name="ContentBorder" CornerRadius="8,8,0,0" Background="#1E1E1E" />

                <!-- Image preview (fit-to-window, zoom with wheel, pan with drag) -->
                <Border x:Name="ImagePanel" Visibility="Collapsed" Background="Transparent"
                        ClipToBounds="True" MouseWheel="ImagePanel_MouseWheel"
                        MouseLeftButtonDown="ImagePanel_MouseDown"
                        MouseMove="ImagePanel_MouseMove"
                        MouseLeftButtonUp="ImagePanel_MouseUp">
                    <Image x:Name="PreviewImage" Stretch="Uniform"
                           RenderOptions.BitmapScalingMode="HighQuality"
                           RenderTransformOrigin="0.5,0.5">
                        <Image.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform x:Name="ImageScale" ScaleX="1" ScaleY="1" />
                                <TranslateTransform x:Name="ImageTranslate" X="0" Y="0" />
                            </TransformGroup>
                        </Image.RenderTransform>
                    </Image>
                </Border>

                <!-- Video/Audio preview -->
                <Grid x:Name="MediaPanel" Visibility="Collapsed">
                    <MediaElement x:Name="MediaPlayer" LoadedBehavior="Manual"
                                  UnloadedBehavior="Stop" Stretch="Uniform"
                                  MediaOpened="MediaPlayer_MediaOpened"
                                  MediaEnded="MediaPlayer_MediaEnded" />

                    <!-- Audio visual -->
                    <StackPanel x:Name="AudioVisual" Visibility="Collapsed"
                                VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="&#x266B;" FontSize="80" Foreground="#42A5F5"
                                   HorizontalAlignment="Center" />
                        <TextBlock x:Name="AudioFileName" Foreground="#CCC" FontSize="16"
                                   HorizontalAlignment="Center" Margin="0,12,0,0" />
                    </StackPanel>

                    <!-- Media controls overlay -->
                    <Border x:Name="MediaControls" VerticalAlignment="Bottom"
                            Background="#CC000000" Padding="12,8" CornerRadius="0,0,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Button x:Name="BtnPlayPause" Content="&#x23F8;" FontSize="18"
                                    Click="PlayPause_Click" Width="36" Height="36"
                                    Background="Transparent" Foreground="White"
                                    BorderThickness="0" Cursor="Hand"
                                    VerticalAlignment="Center" />
                            <!-- Custom seek bar: click anywhere to jump -->
                            <Grid x:Name="SeekBarContainer" Grid.Column="1"
                                  VerticalAlignment="Center" Margin="10,0"
                                  Height="20" Background="Transparent" Cursor="Hand"
                                  MouseLeftButtonDown="SeekTrack_MouseDown"
                                  MouseMove="SeekTrack_MouseMove"
                                  MouseLeftButtonUp="SeekTrack_MouseUp">
                                <Border Background="#44FFFFFF" Height="4" CornerRadius="2"
                                        VerticalAlignment="Center" />
                                <Border x:Name="SeekProgress" Background="#42A5F5" Height="4"
                                        CornerRadius="2" VerticalAlignment="Center"
                                        HorizontalAlignment="Left" Width="0" />
                                <Ellipse x:Name="SeekThumb" Width="12" Height="12"
                                         Fill="White" HorizontalAlignment="Left"
                                         VerticalAlignment="Center" Margin="-6,0,0,0" />
                            </Grid>
                            <TextBlock x:Name="TxtDuration" Grid.Column="2"
                                       Foreground="#AAA" FontSize="12"
                                       VerticalAlignment="Center" Text="0:00 / 0:00" />
                        </Grid>
                    </Border>
                </Grid>

                <!-- Text/Code preview -->
                <FlowDocumentScrollViewer x:Name="TextViewer" Visibility="Collapsed"
                                          VerticalScrollBarVisibility="Auto"
                                          IsToolBarVisible="False"
                                          Background="#1E1E1E"
                                          Zoom="100" />

                <!-- PDF preview -->
                <Border x:Name="PdfPanel" Visibility="Collapsed" Background="#1E1E1E" />

                <!-- Unsupported file info card -->
                <StackPanel x:Name="FileInfoPanel" Visibility="Collapsed"
                            VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock x:Name="FileInfoIcon" Text="&#x1F4C4;" FontSize="64"
                               HorizontalAlignment="Center" Margin="0,0,0,16" />
                    <TextBlock x:Name="FileInfoName" Foreground="#EEE" FontSize="18"
                               FontWeight="SemiBold" HorizontalAlignment="Center"
                               TextWrapping="Wrap" MaxWidth="400" TextAlignment="Center" />
                    <TextBlock x:Name="FileInfoType" Foreground="#888" FontSize="13"
                               HorizontalAlignment="Center" Margin="0,6,0,0" />
                    <TextBlock x:Name="FileInfoSize" Foreground="#888" FontSize="13"
                               HorizontalAlignment="Center" Margin="0,4,0,0" />
                    <TextBlock x:Name="FileInfoDates" Foreground="#666" FontSize="12"
                               HorizontalAlignment="Center" Margin="0,8,0,0"
                               TextAlignment="Center" />
                    <Button x:Name="BtnOpenWith" Content="Open with default app"
                            Margin="0,20,0,0" Padding="16,8" Cursor="Hand"
                            Click="OpenWith_Click"
                            Background="#42A5F5" Foreground="White"
                            BorderThickness="0" FontSize="13" />
                </StackPanel>

                <!-- Loading indicator -->
                <Border x:Name="LoadingOverlay" Visibility="Collapsed"
                        Background="#CC1A1A2E">
                    <TextBlock Text="Loading..." Foreground="#888" FontSize="16"
                               VerticalAlignment="Center" HorizontalAlignment="Center" />
                </Border>
            </Grid>

            <!-- Bottom info bar (drag to move window) -->
            <Border Grid.Row="1" Background="#12FFFFFF" Padding="14,8"
                    CornerRadius="0,0,10,10" Cursor="SizeAll"
                    MouseLeftButtonDown="InfoBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="TxtFileName" Foreground="#CCC" FontSize="12"
                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                    <TextBlock x:Name="TxtFileSize" Grid.Column="1" Foreground="#888"
                               FontSize="12" VerticalAlignment="Center" Margin="16,0" />
                    <Button x:Name="BtnToggleCode" Grid.Column="2" Content="&lt;/&gt;"
                            Visibility="Collapsed" Click="ToggleCode_Click"
                            Cursor="Hand" Margin="0,0,6,0" Padding="8,2"
                            FontSize="11" FontFamily="Consolas" FontWeight="Bold"
                            Background="#33FFFFFF" Foreground="#AAA"
                            BorderThickness="1" BorderBrush="#33FFFFFF"
                            ToolTip="Toggle code / rendered view" />
                    <Button x:Name="BtnFullscreenBar" Grid.Column="3"
                            Click="Fullscreen_Click" Cursor="Hand"
                            Margin="0,0,10,0" Padding="8,2"
                            FontSize="13" Background="#33FFFFFF" Foreground="#AAA"
                            BorderThickness="1" BorderBrush="#33FFFFFF"
                            ToolTip="Toggle fullscreen (F)">
                        <TextBlock x:Name="TxtFullscreenBarIcon" Text="&#x26F6;" />
                    </Button>
                    <TextBlock x:Name="TxtFileMeta" Grid.Column="4" Foreground="#888"
                               FontSize="12" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <!-- Close button (always visible) -->
            <Button x:Name="CloseBtn" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Width="36" Height="36" Margin="8" Click="Close_Click"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
                <Border x:Name="CloseBtnBorder" Background="#99333333" CornerRadius="18"
                        Width="36" Height="36">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#CCE53935" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="&#x2715;" Foreground="White" FontSize="15" FontWeight="Bold"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
            </Button>

            <!-- Navigation arrows -->
            <Button x:Name="BtnPrev" HorizontalAlignment="Left" VerticalAlignment="Center"
                    Width="40" Height="40" Margin="8,0" Click="PrevFile_Click"
                    Cursor="Hand" Opacity="0.6">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="1" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <Border Background="#88222222" CornerRadius="20" Width="40" Height="40">
                    <TextBlock Text="&#x276E;" Foreground="White" FontSize="20"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
            </Button>
            <Button x:Name="BtnNext" HorizontalAlignment="Right" VerticalAlignment="Center"
                    Width="40" Height="40" Margin="8,0" Click="NextFile_Click"
                    Cursor="Hand" Opacity="0.6">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="1" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <Border Background="#88222222" CornerRadius="20" Width="40" Height="40">
                    <TextBlock Text="&#x276F;" Foreground="White" FontSize="20"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
            </Button>
        </Grid>
    </Border>
    </Grid>
</Window>
