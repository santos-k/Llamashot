<Window x:Class="Llamashot.Views.PinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseRightButtonDown="Window_MouseRightButtonDown">
    <Border BorderBrush="#2196F3" BorderThickness="2" CornerRadius="2">
        <Grid>
            <Image x:Name="PinnedImage" Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="HighQuality" />
            <!-- Close button (top-right) -->
            <Button x:Name="CloseBtn"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Width="20" Height="20" Margin="2"
                    Click="Close_Click"
                    Opacity="0"
                    Style="{x:Null}">
                <Border Background="#CC333333" CornerRadius="10">
                    <TextBlock Text="X" Foreground="White" FontSize="10"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
            </Button>
            <!-- Opacity label -->
            <TextBlock x:Name="OpacityLabel"
                       HorizontalAlignment="Left" VerticalAlignment="Bottom"
                       Foreground="White" FontSize="10"
                       Background="#80000000" Padding="4,1"
                       Visibility="Collapsed" />
        </Grid>
    </Border>
</Window>
